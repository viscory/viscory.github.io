{
  "name": "viscory.github.io",
  "private": true,
  "scripts": {
    "dev": "bun build src/main.ts --outdir=public --target=browser --watch & bun x serve public -p 8000 & wait",
    "build": "bun build src/main.ts --outdir=public --target=browser --minify",
    "lint": "bun x oxlint@latest src/main.ts",
    "validate": "bash scripts/validate.sh",
    "check": "bun run lint && bun run build && bun run validate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^0.15.0"
  }
}
